const readXlsxFile = require('read-excel-file/node');

const path = `${__dirname}/resources/report.xlsx`;
readXlsxFile(path, {sheet: 'July 24'}).then((rows) => {
    const results = rows
        .filter((row) => row[0] != null)
        .toSpliced(0,2);

    console.log(results)
  })